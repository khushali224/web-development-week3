<?php require_once '../config/database.php';require_once '../includes/functions.php';$id=(int)($_POST['product_id']??0);$q=max(1,(int)($_POST['quantity']??1));$s=$pdo->prepare('SELECT id FROM products WHERE id=?');$s->execute([$id]);if($s->fetch()){$_SESSION['cart'][$id]=($_SESSION['cart'][$id]??0)+$q;sync_user_cart($pdo);flash('success','Product added to cart.');}else flash('error','Product not found.');go('../cart.php');
